- Initialised PathRare repository.
- Scaffolding basic project setup including the necessary doc files.

- ERROR: auth/configuration-not-found
  CAUSE: Email/Password and Google sign-in providers were not enabled in Firebase Console.
  FIX: Enabled both providers under Authentication → Sign-in method in Firebase Console.

- ERROR: Cross-Origin-Opener-Policy blocking window.closed (Google popup)
  CAUSE: Browser COOP header prevented popup from communicating back with the app.
  FIX: Added `Cross-Origin-Opener-Policy: same-origin-allow-popups` header in next.config.ts.

- ERROR: FirebaseError: Failed to get document because the client is offline.
  CAUSE: Cascade from broken Google popup — Firestore ran with no valid auth session.
  FIX: Resolved by fixing the COOP header above.

- ERROR: FirebaseError: Missing or insufficient permissions.
  CAUSE: Firestore was created in production mode (deny all by default).
  FIX: Updated Firestore Security Rules to allow authenticated users to read/write their own document.

- ERROR: Firebase credentials — auth/configuration-not-found (second occurrence)
  CAUSE: Switched Firebase project from pathrare-55629 to rarity-f316d; providers not enabled on new project.
  FIX: Enabled Email/Password + Google providers on rarity-f316d in Firebase Console.

- ERROR: life-assist/page.tsx line 65 — "Expected ',', got 'ident'"
  CAUSE: Unescaped apostrophes inside single-quoted strings (e.g. 'Doctor's ...' breaks the JS parser).
  FIX: Changed affected string literals to double-quotes (e.g. "Doctor's ...") on lines 65, 72, 79, 103, 111.

-ERROR - ngo task feed not found , coordinator heatmap and tasks not found, volunteer task feed nd stats not found

- ERROR: Email/password signup froze on "Preparing your profile"
  CAUSE: Firebase Auth signs the browser in before the app finishes creating the Firestore `users/{uid}` profile document. The auth provider sometimes checked Firestore during that gap, stored `profile = null`, and `/auth/complete-profile` kept waiting for `profile.role` forever.
  FIX: Added a `refreshProfile()` method to the auth context and call it immediately after the email signup user document is created. The auth listener also avoids replacing a freshly loaded profile with `null` if an earlier Firestore read finishes late. The complete-profile page now shows a recovery message instead of an infinite loading card if a signed-in user still has no saved role.

- CHANGE: Removed Hospital, Doctor, and Coordinator as selectable/authenticated roles.
  DETAILS: The signup role list, role dashboard routing, and complete-profile role-specific forms now only support Patient, NGO, and Volunteer. The old `/coordinator` route files were removed.

- ERROR: Completing the patient profile returned to the same complete-profile form
  CAUSE: The profile save correctly wrote `isProfileComplete: true` to Firestore, but the in-memory auth context still had the older `isProfileComplete: false` profile. The patient layout guard read that stale context value and redirected back to `/auth/complete-profile`.
  FIX: After saving the complete-profile form, the page now calls `refreshProfile()` and only then routes to the role dashboard with `router.replace(...)`.

- TRACKING: User requested that `error.txt` and `command.txt` be updated on each task.
  STATUS: Added logging reminder on 2026-04-24. No new app/runtime error was encountered during this housekeeping update.

- FLOW NOTE: `userflow.txt` was refreshed on 2026-04-24 to match the current support workflow.
  OBSERVATION: NGO assignment currently shortlists a volunteer first; the case is only locked after that volunteer accepts. Patient chat also unlocks in the UI after the volunteer starts the task, not immediately on acceptance.

- UI NOTE: Reduced NGO dashboard screen blinking on 2026-04-24.
  CAUSE: The NGO dashboard provider set `loading = true` on every 10-second background refresh and also cleared visible data on refresh errors, causing cards and sections to flash. FIX: Background polling now refreshes silently after the initial load and preserves the last good data if a background request fails.
