- Next.js scaffolding with Tailwind CSS.
- Basic Authentication UI scaffolding with 3 roles.
- Basic Dashboard layout routing to 4 tabs.
- Two-step auth flow: role selection is a mandatory first step before login/signup form is shown.
- Firebase Authentication: Email/Password + Google Sign-In with Firestore user document creation.
- COOP header fix: `same-origin-allow-popups` added to next.config.ts for Google popup auth.
- Firebase project migrated to rarity-f316d; credentials stored securely in gitignored credentials/ folder.
- Diagnostic Layer scaffold:
    - data/orphanet/xml/ — drop zone for Orphanet XML dataset files (gitignored).
    - data/orphanet/parsed/ — output folder for parsed JSON datasets.
    - scripts/parse_orphanet.py — parses HPO, classification, and alignment XML files to JSON.
    - scripts/embed_symptoms.py — generates Vertex AI text-embedding-004 vectors per disease.
    - scripts/build_index.py — normalises and saves a cosine-similarity index for fast lookup.
    - app/api/diagnose/route.ts — backend inference API: OCR (Vision API) → NLP (Gemini) → embed (Vertex AI) → rank.
    - app/patient/diagnose/page.tsx — frontend UI: upload zone + symptom text mode, pipeline animation, ranked results cards.
    - Diagnose added to patient nav and home page card grid.

[PHASE 2]
- Auth overhaul:
    - 3 role cards for Patient, NGO, and Volunteer.
    - Strict sign-up vs login distinction with role-conflict detection.
    - Google button labels: "Sign up with Google" vs "Login with Google" per mode.
    - Role-specific complete-profile forms (patient/ngo/volunteer).
    - Post-auth routing to role-specific dashboard based on Firestore role field.
- Role-specific dashboards:
    - /patient — home, life-assist, clinical-profile, community, care tabs.
    - /ngo — task batch, demand by region, stats.
    - /volunteer — match-scored task feed, accept interaction, reliability stats.
- Life Assist engine (/patient/life-assist):
    - 4 categories: Financial & Legal, Adaptive Education, Medical & Care, Assistive Technology.
    - Ranked actions by priority score: (Urgency×0.4)+(Risk×0.3)+(Delay×0.2)+(Access×0.1).
    - Multi-tier allocation: Tier1 Volunteer → Tier2 NGO → Tier3 Self-guided → Tier4 Queue.
    - Zero dead-ends: user always gets next best action.
- Backend API routes:
    - /api/tasks — create/get structured task objects.
    - /api/allocate — 4-tier allocation engine with priority scoring.
- /dashboard deprecated: now redirects to role-specific route.
